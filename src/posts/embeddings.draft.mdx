---
title: Embeddings
date: September 21, 4000
description: Finding meaning in a space of numbers
tags: [Machine Learning, Data, Architecture]
---

import {
  EmbeddingCoordinatesDemo,
  EmbeddingSearchDemo,
  EmbeddingJobMatchDemo,
} from "~/components/EmbeddingDemos";

<EmbeddingCoordinatesDemo />

<EmbeddingSearchDemo />

<EmbeddingJobMatchDemo />

An application can embed profile and job summaries in one batch, then rank the
job vectors against a profile vector. The [Batching article's embeddings
section](/batching#embeddings) describes submitting those inputs to the
embeddings endpoint.
